import EditorService from "../src/editor/services/EditorService";
// import * as jquery from "jquery";

// const editorService = new EditorService(jquery);

// @todo implemnt this
describe("editorService", () => {
  it("dummy test", () => {
    /**
    const fixedBody = editorService.getFixedBody("<div>Hello world</div>");

    console.log(fixedBody);
     */
  });
});
